# AI 设计思路：如何让 LLM 读懂"随便写的微信消息"

## 要解决的问题

育苗场的日常记账依赖微信消息——客户发来的内容五花八门：

```
"老王 要800棵西红柿苗 下周三来拉 电话138xxxx"
"张姐定了茄子苗500棵 钱还没给"
"今天给李老板送了300棵辣椒苗到西青 已结"
```

没有固定格式，没有标点规范，错别字常见。传统做法是人工逐条辨认、分类、填入飞书表格，单条耗时 2-3 分钟，每天几十条消息下来，记账本身就变成了体力活。

这个问题用 LLM 天然合适——非结构化文本 → 结构化字段，正是大模型的强项。

## 核心设计决策

### 1. 为什么不用正则而是上 LLM？

正则写死规则处理不了同一语义的十几种说法："没给钱""赊账""未结""欠着""先欠"都表示"未结"。维护规则的成本会超过直接记账的成本。

### 2. 两套 Prompt 而不是一套

记账和订单提取的字段结构差异很大，强行合一会让 Prompt 变长、LLM 混淆。拆成两套独立 System Prompt，由前端 `mode` 参数切换。

### 3. temperature = 0.1

信息提取不需要创造性，0.1 让输出尽可能稳定、可复现。实测 0.1 和 0 差别不大，但 0.1 对极少数模糊情况保留了极小的容错空间。

### 4. response_format: json_object

要求 DeepSeek 直接返回 JSON，省去正则提取的步骤。配合系统 Prompt 中的 JSON Schema 示例，正确率从 85% 提升到 95%+。

## 记账模式 Prompt 设计

```
系统提示词分了三个层次：

第一层：角色设定
"你是一个育苗场记账助手"——把一个通用 LLM 锁定为领域专家角色

第二层：字段定义（收入）
每笔收入提取 8 个字段：location / customer / variety / 
quantityDesc / amount / phone / giftTrial / unpaid

第三层：输出规范
- 金额只取数字，不带"元"字（避免 "200元" 与 200 的解析差异）
- 缺失字段填 "--"（而不是留空，防止前端解析报错）
```

### 最容易出错的字段：unpaid（是否未结）

这是 Prompt 迭代次数最多的字段。用户不会直接说"未结"，而是会说：

| 用户原文 | 需识别为 |
|----------|----------|
| "没给钱" | 是 |
| "赊账" | 是 |
| "月底结" | 是 |
| "已结" | 否 |
| "钱给了" | 否 |
| 什么都不提 | 是（默认为未结——行业惯例） |

前几版 Prompt 没写默认为"是"的规则，导致大量记录被错误标记为已结。加上这一行之后准确率从 70% 跳到了 90%+。

### 第二容易出错：giftTrial（赠苗/试种）

这套系统为家里在跑 SaaS 的育苗场同行预留。用户可能说"送他20棵试试""赠品""免费给了几棵"——需要 Prompt 明确涵盖这些口语表达。

## 订单模式 Prompt 设计

与记账模式不同，订单模式提取的是"客户订了什么、什么时候要"的前置信息。

### 特殊处理：联系方式

加了硬约束"必须是 11 位手机号格式"，因为用户经常把微信号、座机号混在消息里，不加约束 LLM 会一并提取。

## 已知失败模式 & 边界

### 1. 跨消息合并

用户发两条消息描述同一笔交易：
```
消息1："老王"
消息2："西红柿800棵 已结"
```

当前版本**不支持**跨消息合并——每条消息独立解析。如果想解决，需要加一个会话上下文窗口（成本可控）。

### 2. 多客户混淆

一条消息里同时提到多个客户但没有明确分界：
```
"老王的800棵西红柿跟老李的500棵辣椒今天都送了"
```

当前 Prompt 能拆开，但偶尔会把品种对应错。加了一条"一一对应提取，不要交叉匹配"后基本解决。

### 3. 日期缺失

家人经常忘了写日期。前端做了批量日期设置来兜底——用户可以手动统一改。LLM 层面也设了规则：**无日期 → 填当天**。

## 架构选择：为什么用 Cloudflare Workers？

| 方案 | 优点 | 缺点 |
|------|------|------|
| 纯前端直接调 API | 简单 | API Key 暴露 |
| 自建后端服务器 | 灵活 | 运维成本高 |
| **Cloudflare Workers** | Key 安全、免运维、免费额度够用 | 冷启动有延迟 |

对个人小工具来说，Serverless 是最合适的——专注业务逻辑，不用管服务器。

## 效果数据

| 指标 | 上线前 | 上线后 |
|------|--------|--------|
| 单条记账耗时 | 2-3 分钟 | ~10 秒 |
| 字段准确率（人工抽检） | 手动录入也有错 | 95%+ |
| 已稳定运行 | — | 1 个月，处理 200+ 条 |
